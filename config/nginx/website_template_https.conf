## LET'S ENCRYPT BY WEBSTACK.UP
# Standard configuration to load Let's Encrypt-provided certificates
# for the service.
#
# For your convenience, do a "replace all" of these:
#
# localhost -> mydomain.com (no-www)
# /usr/share/nginx/html -> /var/www/...
#
# It is up to you to acquire the actual files.
# Command template:
#
# certbot --email info@localhost --agree-tos certonly --webroot -w /usr/share/nginx/html -d localhost -d www.localhost -d statistiche.localhost
#


## Bogus, self-signed HTTPS Certificate - used for project startup
ssl_certificate /usr/local/turbolab.it/webstackup/autogenerated/ssl_certificate.crt;
ssl_certificate_key /usr/local/turbolab.it/webstackup/autogenerated/ssl_certificate_key.key;


## After you have obtained a valid Let's Encrypt certificate,
# remove the bogus one above and enable this
#ssl_certificate /etc/letsencrypt/live/localhost/fullchain.pem;
#ssl_certificate_key /etc/letsencrypt/live/localhost/privkey.pem;
#ssl_trusted_certificate /etc/letsencrypt/live/localhost/cert.pem;

#add_header Strict-Transport-Security "max-age=31536000;" always;
